# Project Health Report

Summary
- Project: ShadowTalk AI (Vite + React + TypeScript + Tailwind + Supabase)
- Location: repository root

What I inspected
- `package.json` — scripts present (`dev`, `build`, `preview`).
- `vite.config.ts`, `src/` entry points (`src/main.tsx`, `src/App.tsx`).
- `supabase/` folder (functions, migrations, `config.toml`).
- `.env` and `.gitignore` at repo root.

Findings (issues & missing/incomplete items)

- README placeholders: README contains `REPLACE_WITH_PROJECT_ID` placeholders and Lovable links. Replace with real project ID and run instructions specific to this repo.

- Secrets checked into repo: `.env` exists and contains `VITE_SUPABASE_*` values. `.gitignore` does not exclude `.env`. This exposes project-specific keys and should be remedied immediately (see remediation below).

- Supabase function JWT verification: `supabase/config.toml` sets `verify_jwt = false` for many functions. In production, verify JWTs for functions that expect authenticated requests; use server-side service-role keys only where necessary.

- Missing `.env.example` or documentation: there is no `.env.example` documenting required env vars (e.g., `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`). Add one.

- No CI workflows: repository lacks `.github/workflows` (no automatic build/test). Add a simple CI to run `npm ci` and `npm run build` and (if tests added) `npm test`.

- No tests detected: I found no unit/e2e test harness (Vitest/Jest/Cypress). Add at least basic unit tests and a simple smoke test for build.

- Sensitive defaults in code: many client-side calls use `import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY` as fallback bearer token. Ensure server-side functions use server-only keys; never expose service-role keys to clients.

- Missing license / contributing docs: repository doesn't include a `LICENSE` or contributing guidelines. Add as appropriate.


Recommendations (actionable)

1. Immediately rotate exposed keys and remove `.env` from the repository:
   - Revoke and rotate Supabase keys that were exposed.
   - Add `.env` to `.gitignore` and create `.env.example` with placeholders.

2. Harden Supabase function security:
   - Enable `verify_jwt = true` for functions that should only be called by authenticated users.
   - For administrative tasks, use server-side service-role keys stored securely (not in client `.env`).

3. Add documentation and examples:
   - Update `README.md` replacing `REPLACE_WITH_PROJECT_ID` and add a `Setup` section showing required env vars and Supabase setup steps (migrations, functions deployment).
   - Add `CONTRIBUTING.md` and `LICENSE` if applicable.

4. Add CI and tests:
   - Add a GitHub Actions workflow that runs `npm ci`, `npm run build`, and (if tests added) `npm test`.
   - Add a small test suite (Vitest recommended for Vite + TS).

5. Add a `.env.example` file documenting required variables:
   - VITE_SUPABASE_URL
   - VITE_SUPABASE_PUBLISHABLE_KEY
   - (Any server-only keys should be documented but not included)

6. Review runtime behavior and fallbacks:
   - Audit places where client code falls back to `VITE_SUPABASE_PUBLISHABLE_KEY` for Authorization. Ensure those are safe and intended.

7. Optional improvements:
   - Add `LICENSE`, `CODE_OF_CONDUCT.md`, and `SECURITY.md` describing how to report vulnerabilities.
   - Add `Dockerfile` or deployment instructions for production builds.

Next steps I can take (choose any):
- Remove `.env` from the repo and add `.env.example` + update `.gitignore`.
- Create a basic GitHub Actions workflow for build.
- Create `.env.example` and update `README.md` with run steps.

Report generated by repository scan on 2026-01-05.
